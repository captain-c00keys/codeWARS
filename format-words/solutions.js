function formatWords(words){
    if(words === []){
      return "";
    }
    
    if(words.length === 2){
      return words.join(" and ");
    }
    
    if(words.length > 2){
      return words.
  }